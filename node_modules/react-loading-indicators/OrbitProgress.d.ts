import { C as CommonProps } from './common.types.d-a5NbnUWh.js';
import React from 'react';

interface OrbitProgressProps extends CommonProps {
    /**
     * variant of an orbitting loading indicator. Defaults to `disc`.
     */
    variant?: "dotted" | "spokes" | "disc" | "split-disc" | "track-disc";
    /**
     * Make the animation more bold/compact.
     */
    dense?: boolean;
}

declare const OrbitProgress: (props: OrbitProgressProps) => React.JSX.Element | null;

export { type OrbitProgressProps, OrbitProgress as default };
